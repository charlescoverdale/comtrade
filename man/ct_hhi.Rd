% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ct_hhi}
\alias{ct_hhi}
\title{Trade Concentration Index (HHI)}
\usage{
ct_hhi(
  reporter,
  flow = "X",
  year = NULL,
  by = "partner",
  level = 2L,
  cache = TRUE
)
}
\arguments{
\item{reporter}{Character. Reporter country ISO3 code.}

\item{flow}{Character. \code{"X"} or \code{"M"}. Default \code{"X"}.}

\item{year}{Integer. Year to query.}

\item{by}{Character. Concentrate by \code{"partner"} or \code{"product"}.
Default \code{"partner"}.}

\item{level}{Integer. HS digit level (only used when \code{by = "product"}).
Default 2.}

\item{cache}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with columns: year, hhi, concentration, n_items,
top_item, top_share_pct.
}
\description{
Compute the Herfindahl-Hirschman Index measuring the concentration of
a country's trade across partners or products.
}
\details{
HHI ranges from 0 (perfectly diversified) to 10,000 (single
partner/product). Interpretation: < 1,500 = low concentration,
1,500-2,500 = moderate, > 2,500 = high.
}
\examples{
\donttest{
op <- options(comtrade.cache_dir = tempdir())

# Export partner concentration
ct_hhi("AUS", flow = "X", year = 2023, by = "partner")

# Export product concentration
ct_hhi("AUS", flow = "X", year = 2023, by = "product")

options(op)
}
}
