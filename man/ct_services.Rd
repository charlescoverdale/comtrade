% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trade.R
\name{ct_services}
\alias{ct_services}
\title{Get Services Trade Data}
\usage{
ct_services(
  reporter,
  partner = "0",
  service = "TOTAL",
  flow = c("X", "M"),
  year = NULL,
  cache = TRUE
)
}
\arguments{
\item{reporter}{Character. Reporter country ISO3 code.}

\item{partner}{Character. Partner country code. Default \code{"0"} (World).}

\item{service}{Character. EBOPS service code. Default \code{"TOTAL"}.}

\item{flow}{Character. Trade flow: \code{"X"} (exports), \code{"M"} (imports).
Can be a vector. Default \code{c("X", "M")}.}

\item{year}{Integer. Year(s) to query (2000-present). Default: most
recent available year.}

\item{cache}{Logical. Cache results locally. Default \code{TRUE}.}
}
\value{
A data.frame with columns: reporter, reporter_desc, partner,
partner_desc, flow, flow_desc, service_code, service_desc, year,
trade_value_usd.
}
\description{
Download international services trade data from the UN Comtrade database
using the EBOPS (Extended Balance of Payments Services) classification.
}
\examples{
\donttest{
op <- options(comtrade.cache_dir = tempdir())

# UK services exports to the world
ct_services("GBR", year = 2022, flow = "X")

options(op)
}
}
