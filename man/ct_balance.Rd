% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{ct_balance}
\alias{ct_balance}
\title{Trade Balance by Partner}
\usage{
ct_balance(
  reporter,
  partner = "0",
  year = NULL,
  commodity = "TOTAL",
  cache = TRUE
)
}
\arguments{
\item{reporter}{Character. Reporter country ISO3 code.}

\item{partner}{Character. Partner country code, or \code{"0"} for World.
Default \code{"0"}.}

\item{year}{Integer. Year(s) to query.}

\item{commodity}{Character. Commodity code. Default \code{"TOTAL"}.}

\item{cache}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with columns: partner, partner_desc, year, exports,
imports, balance.
}
\description{
Compute the trade balance (exports minus imports) for a reporter country
against each trading partner.
}
\examples{
\donttest{
op <- options(comtrade.cache_dir = tempdir())

# The Comtrade API is rate-limited and can return a non-JSON response
# under load. tryCatch so the example degrades gracefully if that
# happens during the CRAN check.
bal <- tryCatch(ct_balance("GBR", year = 2023), error = function(e) NULL)
if (!is.null(bal)) head(bal)

options(op)
}
}
