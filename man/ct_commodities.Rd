% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{ct_commodities}
\alias{ct_commodities}
\title{Search Commodity Codes}
\usage{
ct_commodities(query = NULL, level = NULL)
}
\arguments{
\item{query}{Character. Search term (matched against commodity
descriptions) or a partial HS code (e.g., \code{"27"} for mineral fuels).
Default \code{NULL} (return all).}

\item{level}{Integer. HS digit level to return. Currently only level 2
is available from the built-in table. Default \code{NULL} (all levels).}
}
\value{
A data.frame with columns:
\describe{
\item{code}{HS 2-digit chapter code}
\item{description}{Chapter description}
\item{level}{Digit level (always 2 for built-in table)}
\item{parent}{Parent code (NA for 2-digit chapters)}
}
}
\description{
Search the HS (Harmonized System) commodity classification for codes
matching a keyword or code pattern. Uses a built-in table of 96
two-digit HS chapters with descriptions.
}
\examples{
# Search for petroleum-related codes
ct_commodities("petroleum")

# List all 2-digit HS chapters
ct_commodities()

# Find codes starting with "27" (mineral fuels)
ct_commodities("27")
}
